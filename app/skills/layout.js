export const metadata = {
  title: 'Job Match',
  description:
    'Paste a job posting and see if your skills and profile are a real match — plus a full skills audit either way.',
};

export default function SkillsLayout({ children }) {
  return children;
}
